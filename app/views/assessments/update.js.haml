:plain
  var html = "#{ escape_javascript(render :partial => 'assessments/form',
    locals: { student: @assessment.student,
              assessment: @assessment,
              objectives: @assessment.assessor.objectives }) } }"

  $("table#student_#{ @assessment.student_id }").replaceWith(function() {
      return $(html).hide().fadeIn();
  });
