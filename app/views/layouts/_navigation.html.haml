- if current_teacher
  %ul.nav.navbar-nav
    %li.dropdown
      = link_to groups_path do
        Classes
    %li.dropdown
      = link_to indicators_path do
        Subjects
  %ul.nav.navbar-nav.navbar-right.teacher-links
    %li
      = link_to '#', { class: 'dropdown-toggle', data: { toggle: 'dropdown' } } do
        = current_teacher
        %span.caret
      %ul.dropdown-menu
        %li= link_to 'Edit profile', edit_teacher_registration_path
        %li= link_to 'Invite colleagues', new_teacher_invitation_path
        %li.divider
        %li= link_to "Sign out", destroy_teacher_session_path, :method => :delete
- else
  %ul.nav.navbar-nav.navbar-right
    /  %li= link_to "Register", new_teacher_registration_path
    %li= link_to "Sign in (beta testers)", new_teacher_session_path
