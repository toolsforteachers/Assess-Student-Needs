- @page_header = link_to_group

%p
  =link_to group_path(@group) do
    = glyph('Back', 'triangle-left')

= bootstrap_nested_form_for(@group) do |f|
  = render partial: 'groups/students_fields', locals: { f: f}
