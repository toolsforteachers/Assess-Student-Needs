- if @group.lessons.empty?
  .bs-component
    .jumbotron
      %p You don't have any lessons yet.
      = link_to new_group_lesson_path(@group) do
        .btn.btn-primary.btn-lg
          Add a new lesson

- else
  .list-group
    - @group.lessons.each do |lesson|
      .list-group-item
        %span.badge= lesson.lesson_date.to_s
        = link_to lesson.name, group_lesson_path(@group, lesson)
        + list of indicators
        + date
        = link_to edit_group_lesson_path(@group, lesson) do
          .span.glyphicon.glyphicon-cog
          Edit lesson
